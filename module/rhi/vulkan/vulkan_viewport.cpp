#include "vulkan_viewport.h"

namespace source_module::rhi
{
	vulkan_viewport::vulkan_viewport(vulkan_device* device, void* window_handle, uint32_t size_x, uint32_t size_y, bool is_fullscreen, source_runtime::core::pixel_format preferred_pixel_format)
	{
		
	}

}
